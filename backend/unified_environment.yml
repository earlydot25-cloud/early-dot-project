name: early_dot_env
channels:
  - conda-forge   # 👈 1순위: 안정적인 범용 패키지를 먼저 찾도록 조정
  - pytorch       # 👈 2순위: PyTorch 관련 바이너리를 찾도록
  - defaults
dependencies:
  - python=3.10
  - numpy
  # PyTorch 관련 패키지
  - pytorch
  - torchvision
  - torchaudio

  - opencv        # Conda 빌드의 opencv

  # - sympy=1.13.1  # 👈 주석 처리: 종속성 해결 실패의 주범일 수 있으므로 제거
  - fsspec
  - mysqlclient

  - pip
#  - pip:
#    # 이 파일은 backend/requirements_base.txt 파일입니다.
#    - -r requirements_base.txt